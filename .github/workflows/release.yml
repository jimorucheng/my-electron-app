name: Build and Release Electron App

# 仅在推送 tag 时触发，例如 v1.0.0
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Make macOS dmg/zip
  #       run: npm run make -- --platform=darwin --arch=x64

  #     - name: Upload macOS artifacts
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: macos-artifacts
  #         path: out/make/**

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Make Windows exe
        run: npm run make -- --platform=win32 --arch=x64

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: out/make/**

  # build-linux:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20

  #     - name: Install dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y rpm fakeroot
  #         npm install

  #     - name: Make Linux deb/rpm
  #       run: npm run make -- --platform=linux --arch=x64

  #     - name: Upload Linux artifacts
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: linux-artifacts
  #         path: out/make/**

  # needs: [build-macos, build-windows, build-linux]

  release:
    runs-on: ubuntu-latest
    needs: [build-windows]
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}  # 使用你的 GitHub Token